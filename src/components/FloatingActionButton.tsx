import React from 'react';

// Component currently disabled for mobile devices as requested.
// Previously contained a fixed '?' support button.
export const FloatingActionButton = () => {
  return null;
};
